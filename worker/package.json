{
  "name": "estudyou-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone background worker for eStudyou: marks past-due scheduled classes as completed and generates recurring class occurrences. Deliberately has no dependency of its own on @prisma/client/prisma — it resolves the already-generated client from the root project's node_modules (run `pnpm install` at the repo root first), avoiding a second, conflicting client generation.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
